export function SetupPreviewPage() {
  return (
    <main className="setup-preview-page">
      <section className="setup-preview-card">
        <p className="eyebrow">Smart Parking System</p>
        <h1>Admin dashboard scaffold initialized</h1>
        <p>
          The React + TypeScript prototype shell is now in place under <strong>admin-app</strong> with
          architecture-aligned folders for pages, components, hooks, utilities, and services.
        </p>
        <ul>
          <li>Step 1 establishes the web scaffold and build tooling.</li>
          <li>Step 2 will add the prototype access gate and admin shell navigation.</li>
          <li>Step 3 will prepare parking area and slot views with Firestore read readiness.</li>
        </ul>
      </section>
    </main>
  );
}
